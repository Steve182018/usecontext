import React from "react";
import { useContext } from "react";
import { GlobalInfo } from "../App";
const Other_Child = () =>{

    // const {AppColor} = useContext(GlobalInfo)

    return(
        <>
            <h1>Other Child Component</h1>
        </>
    )
}

export default Other_Child;